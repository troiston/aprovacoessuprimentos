/** Nome do cookie httpOnly com o token da tabela `Session.sessionToken`. */
export const SESSION_COOKIE_NAME = "incorporacao_session";

/** Duração da sessão em dias. */
export const SESSION_MAX_AGE_DAYS = 7;
